// To use this file:
// 1. Press Ctrl + ` to open the shell
// 2. Copy this command `gcc test_RootFinding.c -o test_RootFinding.exe`
// 3. Paste it into the shell with Ctrl + Shift + V and press Enter to run the command
// 3. Run this command `./test_RootFinding.exe` (using the same steps in #2-3)

#include "RootFinding.c"

int main() {
    RootFinding();
    return 0;
}
